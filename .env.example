# Example environment variables for Chugli
# Copy this file to `.env` and fill in real values for your environment.

# Backend
# NODE_ENV should be 'development' or 'production'
NODE_ENV=development
# Port on which the backend server will run
PORT=3000
# CORS origin for the frontend app (set to your frontend origin)
CORS_ORIGIN=http://localhost:5173
# Redis connection
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Frontend (Vite)
# Vite environment variables must be prefixed with VITE_
VITE_API_URL=http://localhost:3000

# Notes:
# - Keep `.env` out of source control. This file is safe to commit.
# - For production, set these values in your deployment environment (e.g., Docker, host env, or CI).
